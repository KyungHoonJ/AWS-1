# sequelize-typescript

```bash
npm i sequelize-typescript

npx ts-node ./src/decoTest.ts
```

- typescript에서 sequelize를 편하게 쓰기 위한 라이브러리

# Decorator

- 코드를 꾸며주는 코드
- 미들웨어? -> 중간단계
- 앞에 달린 코드

```ts
@test
class Test {}
```

```sql
CREATE USER 'jkh'@'localhost' IDENTIFIED WITH mysql_native_password BY '1234qwER!@';

CREATE DATABASE jkh;

GRANT ALL PRIVILEGES ON jkh.* TO 'jkh'@'localhost';
```
